"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Wechaty - Wechat for Bot. Connecting ChatBots
 *
 * Interface for puppet
 *
 * Licenst: ISC
 * https://github.com/zixia/wechaty
 *
 */
const psTree = require('ps-tree');
const events_1 = require("events");
const state_switch_1 = require("state-switch");
/* tslint:disable:no-var-requires */
const retryPromise = require('retry-promise').default; // https://github.com/olalonde/retry-promise
const config_1 = require("../config");
const brolog_env_1 = require("../brolog-env");
const browser_cookie_1 = require("./browser-cookie");
const browser_driver_1 = require("./browser-driver");
class Browser extends events_1.EventEmitter {
    constructor(setting = {
            head: config_1.Config.head,
            sessionFile: '',
        }) {
        super();
        this.setting = setting;
        this.state = new state_switch_1.StateSwitch('Browser', 'close', brolog_env_1.default);
        brolog_env_1.default.verbose('PuppetWebBrowser', 'constructor() with head(%s) sessionFile(%s)', setting.head, setting.sessionFile);
        this.driver = new browser_driver_1.default(this.setting.head);
        this.cookie = new browser_cookie_1.BrowserCookie(this.driver, this.setting.sessionFile);
    }
    toString() { return `Browser({head:${this.setting.head})`; }
    init() {
        return __awaiter(this, void 0, void 0, function* () {
            brolog_env_1.default.verbose('PuppetWebBrowser', 'init()');
            /**
             * do not allow to init() twice without quit()
             */
            if (this.state.current() === 'open') {
                let e;
                if (this.state.inprocess()) {
                    e = new Error('init() fail: current state is `open`-`ing`');
                }
                else {
                    e = new Error('init() fail: current state is `open`');
                }
                brolog_env_1.default.error('PuppetWebBrowser', e.message);
                throw e;
            }
            this.state.target('open');
            this.state.current('open', false);
            this.hostname = this.cookie.hostname();
            // jumpUrl is used to open in browser for we can set cookies.
            // backup: 'https://res.wx.qq.com/zh_CN/htmledition/v2/images/icon/ico_loading28a2f7.gif'
            const jumpUrl = `https://${this.hostname}/zh_CN/htmledition/v2/images/webwxgeticon.jpg`;
            try {
                yield this.driver.init();
                brolog_env_1.default.verbose('PuppetWebBrowser', 'init() driver.init() done');
                yield this.open(jumpUrl);
                yield this.loadCookie()
                    .catch(e => {
                    brolog_env_1.default.verbose('PuppetWebBrowser', 'browser.loadSession(%s) exception: %s', this.setting.sessionFile, e && e.message || e);
                });
                yield this.open();
                /**
                 * when open url, there could happen a quit() call.
                 * should check here: if we are in `close` target state, we should clean up
                 */
                if (this.state.target() !== 'open') {
                    throw new Error('init() open() done, but state.target() is set to close after that. has to quit().');
                }
                this.state.current('open');
                return;
            }
            catch (err) {
                brolog_env_1.default.error('PuppetWebBrowser', 'init() exception: %s', err.message);
                yield this.quit();
                throw err;
            }
        });
    }
    open(url = `https://${this.hostname}`) {
        return __awaiter(this, void 0, void 0, function* () {
            brolog_env_1.default.verbose('PuppetWebBrowser', `open(${url})`);
            if (!this.hostname) {
                throw new Error('hostname unknown');
            }
            // TODO: set a timer to guard driver.get timeout, then retry 3 times 201607
            try {
                yield this.driver.get(url);
            }
            catch (e) {
                brolog_env_1.default.error('PuppetWebBrowser', 'open() exception: %s', e.message);
                throw e;
            }
        });
    }
    refresh() {
        return __awaiter(this, void 0, void 0, function* () {
            brolog_env_1.default.verbose('PuppetWebBrowser', 'refresh()');
            yield this.driver
                .navigate()
                .refresh();
            return;
        });
    }
    restart() {
        return __awaiter(this, void 0, void 0, function* () {
            brolog_env_1.default.verbose('PuppetWebBrowser', 'restart()');
            yield this.quit();
            if (this.state.current() === 'open' && this.state.inprocess()) {
                brolog_env_1.default.warn('PuppetWebBrowser', 'restart() found state.current() === open and inprocess() after quit()!');
                return;
            }
            yield this.init();
        });
    }
    quit() {
        return __awaiter(this, void 0, void 0, function* () {
            brolog_env_1.default.verbose('PuppetWebBrowser', 'quit()');
            if (this.state.current() === 'close') {
                let e;
                if (this.state.inprocess()) {
                    e = new Error('quit() fail: on a browser with state.current():`close` and inprocess():`true` ?');
                }
                else {
                    e = new Error('quit() fail: on a already quit-ed browser');
                }
                brolog_env_1.default.warn('PuppetWebBrowser', e.message);
                throw e;
            }
            this.state.target('close');
            this.state.current('close', false);
            try {
                yield this.driver.close()
                    .catch(e => { }); // http://stackoverflow.com/a/32341885/1123955
                brolog_env_1.default.silly('PuppetWebBrowser', 'quit() driver.close() done');
                yield this.driver.quit()
                    .catch(e => brolog_env_1.default.error('PuppetWebBrowser', 'quit() this.driver.quit() exception %s', e.message));
                brolog_env_1.default.silly('PuppetWebBrowser', 'quit() driver.quit() done');
                /**
                 *
                 * if we use AVA test runner, then this.clean might cause problems
                 * because there will be more than one instance of browser with the same nodejs process id
                 *
                 */
                try {
                    yield this.clean();
                }
                catch (e) {
                    yield this.clean(true);
                }
            }
            catch (e) {
                // log.warn('PuppetWebBrowser', 'err: %s %s %s %s', e.code, e.errno, e.syscall, e.message)
                brolog_env_1.default.warn('PuppetWebBrowser', 'quit() exception: %s', e.message);
                const crashMsgs = [
                    'ECONNREFUSED',
                    'WebDriverError: .* not reachable',
                    'NoSuchWindowError: no such window: target window already closed',
                ];
                const crashRegex = new RegExp(crashMsgs.join('|'), 'i');
                if (crashRegex.test(e.message)) {
                    brolog_env_1.default.warn('PuppetWebBrowser', 'driver.quit() browser crashed');
                }
                else {
                    brolog_env_1.default.warn('PuppetWebBrowser', 'driver.quit() exception: %s', e.message);
                }
                /* fail safe */
            }
            this.state.current('close');
            return;
        });
    }
    clean(kill = false) {
        return __awaiter(this, void 0, void 0, function* () {
            brolog_env_1.default.verbose('PuppetWebBrowser', 'clean(kill=%s)', kill);
            const max = 15;
            const backoff = 100;
            /**
             * issue #86 to kill orphan browser process
             */
            if (kill) {
                const pidList = yield this.getBrowserPidList();
                brolog_env_1.default.verbose('PuppetWebBrowser', 'clean() %d browsers will be killed', pidList.length);
                pidList.forEach(pid => {
                    try {
                        process.kill(pid, 'SIGKILL');
                    }
                    catch (e) {
                        brolog_env_1.default.warn('PuppetWebBrowser', 'clean(kill=true) process.kill(%d, SIGKILL) exception: %s', pid, e.message);
                    }
                });
            }
            /**
             * max = (2*totalTime/backoff) ^ (1/2)
             * timeout = 45000 for {max: 30, backoff: 100}
             * timeout = 11250 for {max: 15, backoff: 100}
             */
            const timeout = max * (backoff * max) / 2;
            return retryPromise({ max, backoff }, (attempt) => __awaiter(this, void 0, void 0, function* () {
                brolog_env_1.default.silly('PuppetWebBrowser', 'clean() retryPromise: attempt %s time for timeout %s', attempt, timeout);
                const pidList = yield this.getBrowserPidList();
                if (pidList.length > 0) {
                    throw new Error('browser number: ' + pidList.length);
                }
            }));
        });
    }
    getBrowserPidList() {
        brolog_env_1.default.verbose('PuppetWebBrowser', 'getBrowserPidList()');
        const head = this.setting.head;
        return new Promise((resolve, reject) => {
            /**
             * Reject
             */
            const timer = setTimeout(() => {
                const e = new Error('clean() psTree() timeout.');
                brolog_env_1.default.error('PuppetWebBrowser', e.message);
                reject(e);
            }, 10 * 1000);
            psTree(process.pid, (err, children) => {
                if (err) {
                    brolog_env_1.default.error('PuppetWebBrowser', 'getBrowserPidList() %s', err.message || err);
                    reject(err);
                    return;
                }
                let browserRe;
                switch (head) {
                    case 'phantomjs':
                        browserRe = 'phantomjs';
                        break;
                    case 'chrome':
                        browserRe = 'chrome(?!driver)|chromium';
                        break;
                    default:
                        const e = new Error('unsupported head: ' + head);
                        brolog_env_1.default.warn('PuppetWebBrowser', 'getBrowserPids() for %s', e.message);
                        throw e;
                }
                const matchRegex = new RegExp(browserRe, 'i');
                const pids = children.filter(child => {
                    // https://github.com/indexzero/ps-tree/issues/18
                    if (matchRegex.test('' + child.COMMAND + child.COMM)) {
                        brolog_env_1.default.silly('PuppetWebBrowser', 'getBrowserPids() child: %s', JSON.stringify(child));
                        return true;
                    }
                    return false;
                }).map(child => child.PID);
                /**
                 * Resolve
                 */
                clearTimeout(timer);
                resolve(pids);
                return;
            });
        });
    }
    execute(script, ...args) {
        return __awaiter(this, arguments, void 0, function* () {
            brolog_env_1.default.silly('PuppetWebBrowser', 'Browser.execute("%s")', (script.slice(0, 80)
                .replace(/[\n\s]+/g, ' ')
                + (script.length > 80 ? ' ... ' : '')));
            // log.verbose('PuppetWebBrowser', `Browser.execute() driver.getSession: %s`, util.inspect(this.driver.getSession()))
            if (this.dead()) {
                const e = new Error('Browser.execute() browser dead');
                brolog_env_1.default.warn('PuppetWebBrowser', 'execute() this.dead() %s', e.stack);
                throw e;
            }
            try {
                return yield this.driver.executeScript.apply(this.driver, arguments);
            }
            catch (e) {
                // this.dead(e)
                brolog_env_1.default.silly('PuppetWebBrowser', 'execute() script: %s', script);
                brolog_env_1.default.warn('PuppetWebBrowser', 'execute() exception: %s, %s', e.message.substr(0, 99), e.stack);
                throw e;
            }
        });
    }
    executeAsync(script, ...args) {
        return __awaiter(this, arguments, void 0, function* () {
            brolog_env_1.default.silly('PuppetWebBrowser', 'Browser.executeAsync(%s)', script.slice(0, 80));
            if (this.dead()) {
                throw new Error('browser dead');
            }
            try {
                return yield this.driver.executeAsyncScript.apply(this.driver, arguments);
            }
            catch (e) {
                // this.dead(e)
                brolog_env_1.default.warn('PuppetWebBrowser', 'executeAsync() exception: %s', e.message.slice(0, 99));
                throw e;
            }
        });
    }
    /**
     *
     * check whether browser is full functional
     *
     */
    readyLive() {
        return __awaiter(this, void 0, void 0, function* () {
            brolog_env_1.default.verbose('PuppetWebBrowser', 'readyLive()');
            if (this.dead()) {
                brolog_env_1.default.silly('PuppetWebBrowser', 'readyLive() dead() is true');
                return false;
            }
            let two;
            try {
                two = yield this.execute('return 1+1');
            }
            catch (e) {
                two = e && e.message;
            }
            if (two === 2) {
                return true; // browser ok, living
            }
            const errMsg = 'found dead browser coz 1+1 = ' + two + ' (not 2)';
            brolog_env_1.default.warn('PuppetWebBrowser', 'readyLive() %s', errMsg);
            this.dead(errMsg);
            return false; // browser not ok, dead
        });
    }
    dead(forceReason) {
        // too noisy!
        // log.silly('PuppetWebBrowser', 'dead() checking ... ')
        if (this.state.target() === 'close'
            || this.state.current() === 'close') {
            brolog_env_1.default.verbose('PuppetWebBrowser', 'dead() state target(%s) current(%s) stable(%s)', this.state.target(), this.state.current(), this.state.stable());
            brolog_env_1.default.verbose('PuppetWebBrowser', 'dead() browser is in dead state');
            return true;
        }
        let msg;
        let dead = false;
        if (forceReason) {
            dead = true;
            msg = forceReason;
            brolog_env_1.default.verbose('PuppetWebBrowser', 'dead(forceReason=%s) %s', forceReason, new Error().stack);
        }
        else if (!this.driver) {
            dead = true;
            msg = 'no driver or session';
        }
        if (dead) {
            brolog_env_1.default.warn('PuppetWebBrowser', 'dead(%s) because %s', forceReason
                ? forceReason
                : '', msg);
            if (this.state.target() === 'open'
                && this.state.current() === 'open'
                && this.state.stable()) {
                brolog_env_1.default.verbose('PuppetWebBrowser', 'dead() emit a `dead` event because %s', msg);
                this.emit('dead', msg);
            }
            else {
                brolog_env_1.default.warn('PuppetWebBrowser', 'dead() wil not emit `dead` event because states are: target(%s), current(%s), stable(%s)', this.state.target(), this.state.current(), this.state.stable());
            }
        }
        return dead;
    }
    addCookie(cookie) {
        return this.cookie.add(cookie);
    }
    saveCookie() { return this.cookie.save(); }
    loadCookie() { return this.cookie.load(); }
    readCookie() { return this.cookie.read(); }
    cleanCookie() { return this.cookie.clean(); }
}
exports.Browser = Browser;
exports.default = Browser;
//# sourceMappingURL=browser.js.map